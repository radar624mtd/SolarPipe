# Build outputs
bin/
obj/
*.user
.vs/

# Logs and test outputs
logs/
logs/*.json

# Model registry (large binary artifacts)
models/registry/

# Python sidecar virtualenv
python/.venv/
python/__pycache__/
python/**/__pycache__/
python/**/*.pyc

# Cached data
cache/

# Database files (solar_data.db is ~10 GB — never commit)
*.db

# Secrets and local settings
*.env
appsettings.local.json
.claude/settings.local.json

# Claude Code workspace memory (machine-local, not for repo)
.claude/
data/.claude/

# Data pipeline — raw cache, staging DB, output artifacts
data/data/raw/
data/data/staging/
data/data/output/

# OS artifacts
.DS_Store
Thumbs.db
